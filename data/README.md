# seed 데이터

`seed.json` 은 식품안전나라 API가 죽은 상태에서 서버를 배포해도 빈 화면 대신
최근 회수 데이터를 보여주기 위한 폴백 스냅샷이다.

API가 살아있을 때 아래 명령으로 갱신한다:

    FOODSAFETYKOREA_API_KEY=발급키 npm run seed

실데이터(주기 갱신)가 성공하면 seed 는 즉시 덮어써지므로, 오래된 정보가
서비스에 계속 노출되지 않는다. seed 는 부팅 순간의 안전망일 뿐이다.
