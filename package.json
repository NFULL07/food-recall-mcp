{
  "name": "food-recall-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "식자재회수점검 (foodRecall) - MFDS food recall lot-matching MCP server",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "probe": "node scripts/probe.mjs",
    "test:match": "tsx scripts/test-match.ts",
    "seed": "node scripts/make-seed.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.1.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
